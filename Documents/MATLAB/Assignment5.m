original;
sum(p(x,y));
sum(q(x,y));
D1;
saliency_map;
meansquareerror
qualityMatrix = ((p(x,y)-q(x,y))^2)* map(x,y);
structureSimularity;
meanSquareError
PSNR
SSIM(x,y);
distortion1
distortion2
pic1
pic2
pic3

% normalize ???
% Code in black board for saliency tool
% click on the link to download and load code.

%run install
% sample images in demo 
% 
